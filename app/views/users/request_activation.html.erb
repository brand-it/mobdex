<div id="information">
	<%= form_for @user, :url => resend_activation_users_path do |f| %>	
		<div class="text_field">
			<%= f.label :email %><br />
			<%= f.text_field :email %>
		</div>
		<div class="submit">
			<%= f.submit "Send Activation" %>
		</div>
	<% end %>
</div>
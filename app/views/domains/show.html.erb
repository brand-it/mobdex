<div id="information">
	<h1><%= @domain.title %></h1>
	<p><%= @domain.description %></p>
	<div><%= @domain.url %></div>
	<div>
		<% for tag in @domain.tags %>
			<p><%= tag.name %></p>
		<% end %>
	</div>
</div>

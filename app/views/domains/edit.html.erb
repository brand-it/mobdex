<div id="information">
	<h1>Edit Domain <%= @domain.name %></h1>

	<div class="form">
		<%= form_for(@domain) do |f| %>
	  		<%= render "form", :form => f %>
	  		<%= f.submit %>
		<% end %>
	</div>
</div>